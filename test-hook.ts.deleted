// test-hook.ts
// This file tests if the TypeScript hook is working

// This should trigger the hook - using 'any' type
const testData = (await fetch('/api/test').json()) as any;

// This should trigger ESLint - console.log is forbidden in your project
console.log(testData);

// This should trigger - using 'var' instead of 'const'
var oldStyleVariable = "test";

// This should trigger - object possibly undefined with strict mode
interface User {
  profile?: {
    name: string;
    address?: {
      street: string;
    };
  };
}

const user: User = { profile: { name: "Test" } };
const userStreet = user.profile.address.street; // 'address' is possibly undefined

export { testData };
