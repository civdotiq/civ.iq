# Civic Intel Hub - Environment Variables Template
# Copy this file to .env.local and replace dummy values with your actual API keys

# =============================================================================
# Required API Keys for Full Functionality
# =============================================================================

# Congress.gov API Key (REQUIRED for bills, votes, and member data)
# Get your free API key at: https://api.congress.gov/sign-up/
# Documentation: https://github.com/LibraryOfCongress/api.congress.gov
CONGRESS_API_KEY=your_congress_api_key_here

# FEC API Key (REQUIRED for campaign finance data)
# Get your free API key at: https://api.open.fec.gov/developers/
# Documentation: https://api.open.fec.gov/developers/
FEC_API_KEY=your_fec_api_key_here

# OpenStates API Key (REQUIRED for state legislature data)
# Get your free API key at: https://openstates.org/accounts/profile/
# Documentation: https://docs.openstates.org/api-v3/
OPENSTATES_API_KEY=your_openstates_api_key_here

# US Census Bureau API Key (REQUIRED for demographic and district data)
# Get your free API key at: https://api.census.gov/data/key_signup.html
# Documentation: https://www.census.gov/data/developers/guidance/api-user-guide.html
CENSUS_API_KEY=your_census_api_key_here

# =============================================================================
# Optional Configuration
# =============================================================================

# =============================================================================
# Redis/Cache Configuration (Optional - improves performance)
# =============================================================================
#
# CIV.IQ supports three caching modes:
# 1. Upstash Redis REST API (RECOMMENDED for production/serverless)
# 2. Traditional Redis via ioredis (Good for development/dedicated servers)
# 3. In-memory fallback (Automatic if no Redis configured)
#
# See docs/architecture/CACHE_ARCHITECTURE.md for details

# Option 1: Upstash Redis REST API (Recommended for Vercel/serverless)
# Get free account at: https://upstash.com/
# Benefits: HTTP-based, no persistent connections, serverless-optimized
UPSTASH_REDIS_REST_URL=https://your-redis-instance.upstash.io
UPSTASH_REDIS_REST_TOKEN=your_upstash_token_here

# Option 2: Traditional Redis (Alternative for development)
# Either set REDIS_URL (single connection string) OR the discrete host/port vars.
# Leave blank to skip traditional Redis.
# REDIS_URL=redis://localhost:6379
REDIS_HOST=localhost
REDIS_PORT=6379
REDIS_PASSWORD=
REDIS_DB=0

# OpenAI API Key (Optional - legacy fallback for AI bill summaries)
# Get your API key at: https://platform.openai.com/api-keys
# Note: This feature works without the key using fallback summaries
OPENAI_API_KEY=your_openai_api_key_here

# Google Gemini AI (RECOMMENDED - free tier: 250 req/day Flash, 1000 req/day Flash-Lite)
# Get your free API key at: https://aistudio.google.com/apikey
# This is now the primary AI provider. OpenAI above serves as fallback.
GOOGLE_GENERATIVE_AI_API_KEY=your_gemini_api_key_here

# AI model selection (optional, defaults to gemini-2.5-flash-lite)
# Options: gemini-2.5-flash-lite (recommended), gemini-2.5-flash (higher quality, higher cost)
# AI_MODEL=gemini-2.5-flash-lite

# Ollama base URL (optional - use a local Ollama instance as the AI provider)
# Defaults to http://localhost:11434 when the Ollama provider is selected.
# OLLAMA_BASE_URL=http://localhost:11434

# =============================================================================
# Application Configuration
# =============================================================================

# Environment (development, staging, production)
NODE_ENV=development

# Application URL (used for internal API calls)
# In production, set this to your actual domain
NEXT_PUBLIC_APP_URL=http://localhost:3001

# Congressional Session Configuration (119th Congress - 2025-2027)
CURRENT_CONGRESS=119
CONGRESS_START_DATE=2025-01-03
CONGRESS_END_DATE=2027-01-03

# Enable/disable certain features
ENABLE_ANALYTICS=false
ENABLE_ERROR_REPORTING=false

# FRED Economic Data (Optional - state/metro economic indicators)
# Get your free API key at: https://fredaccount.stlouisfed.org/apikeys
# Documentation: https://fred.stlouisfed.org/docs/api/fred/
FRED_API_KEY=your_fred_api_key_here

# Regulations.gov / Data.gov (Optional - public comments on proposed rules)
# Get your free API key at: https://api.data.gov/signup/
# Documentation: https://open.gsa.gov/api/regulationsgov/
DATA_GOV_API_KEY=your_data_gov_api_key_here

# HUD User API (Optional - Fair Market Rents and Income Limits)
# Register and get your token at: https://www.huduser.gov/hudapi/public/register
# Documentation: https://www.huduser.gov/portal/dataset/fmr-api.html
HUD_API_TOKEN=your_hud_api_token_here

# EIA (Energy Information Administration) API Key (Optional - state energy profiles)
# Get your free API key at: https://www.eia.gov/opendata/register.php
# Documentation: https://www.eia.gov/opendata/documentation.php
EIA_API_KEY=your_eia_api_key_here

# NOAA CDO (Climate Data Online) Token (Optional - climate normals, severe weather)
# Get your free token at: https://www.ncdc.noaa.gov/cdo-web/token
# Documentation: https://www.ncdc.noaa.gov/cdo-web/webservices/v2
NOAA_TOKEN=your_noaa_token_here

# CourtListener API Token (Optional - federal court opinions and dockets)
# Get your free token at: https://www.courtlistener.com/help/api/rest/
# Documentation: https://www.courtlistener.com/help/api/rest/
COURTLISTENER_API_TOKEN=your_courtlistener_token_here

# DOL (Department of Labor) API Key (Optional - OSHA enforcement data)
# Get your free key at: https://dataportal.dol.gov/
# Documentation: https://dataportal.dol.gov/registration
DOL_API_KEY=your_dol_api_key_here

# FollowTheMoney.org API (Optional - state campaign finance data)
# Get your free API key at: https://www.followthemoney.org/our-data/apis
# Note: This feature works without the key - returns empty finance data gracefully
FOLLOWTHEMONEY_API_KEY=your_followthemoney_api_key_here

# NewsAPI (Optional - news aggregation)
# Get your API key at: https://newsapi.org/register
# NEWSAPI_KEY=your_newsapi_key_here

# Radar.io (Optional - address autocomplete and geolocation)
# Get your free API key at: https://radar.com/signup
# NEXT_PUBLIC_RADAR_API_KEY=your_radar_api_key_here

# =============================================================================
# Email Alerts (Optional - civic alert subscriptions via Resend)
# =============================================================================

# Resend API key (sends subscription confirmation + alert emails)
# Get your key at: https://resend.com/api-keys
# If unset, the alerts subscribe endpoint accepts signups but sends no email.
# RESEND_API_KEY=your_resend_api_key_here

# Secret for signing unsubscribe/confirmation tokens (HMAC). Required if alerts
# are enabled. Generate: openssl rand -base64 32
# ALERT_TOKEN_SECRET=your_alert_token_secret_here

# Physical mailing address for the CAN-SPAM footer in alert emails.
# ALERT_PHYSICAL_ADDRESS=CIV.IQ, 123 Main St, Anytown, ST 00000

# =============================================================================
# Nostr Publishing (Optional - publishes civic events to the Nostr network)
# =============================================================================

# Nostr private key (64-char hex). Generate one:
#   node -e "const{generateSecretKey,getPublicKey}=require('nostr-tools/pure');const sk=generateSecretKey();console.log(Array.from(sk).map(b=>b.toString(16).padStart(2,'0')).join(''))"
# If not set, the Nostr publisher cron runs but publishes nothing.
# NOSTR_PRIVATE_KEY=

# Comma-separated relay WebSocket URLs (optional, defaults to 7 public relays)
# NOSTR_RELAYS=wss://relay.damus.io,wss://nos.lol,wss://relay.nostr.band

# GovInfo API Key (Optional - used by Nostr publisher for hearing detection)
# Default: DEMO_KEY (40 req/hr). Get a key at: https://api.govinfo.gov/docs
# GOVINFO_API_KEY=your_govinfo_api_key_here

# =============================================================================
# ActivityPub Federation (Optional - fediverse instances can follow civic updates)
# =============================================================================

# Domain for ActivityPub actor (defaults to civ.iq)
# ACTIVITYPUB_DOMAIN=civ.iq

# RSA keypair for HTTP Signatures (required for federation)
# Generate with: node -e "const crypto=require('crypto');const{publicKey,privateKey}=crypto.generateKeyPairSync('rsa',{modulusLength:2048});console.log(privateKey.export({format:'pem',type:'pkcs1'}));console.log(publicKey.export({format:'pem',type:'spki'}))"
# Store as single-line with \n for newlines:
# ACTIVITYPUB_PRIVATE_KEY=-----BEGIN RSA PRIVATE KEY-----\nMIIE...
# ACTIVITYPUB_PUBLIC_KEY=-----BEGIN PUBLIC KEY-----\nMIIB...

# =============================================================================
# Third-party Services (No API keys required)
# =============================================================================

# The following services work without API keys:
# - SEC EDGAR API (financial disclosures, requires User-Agent header only)
# - GDELT Project API (news data)
# - Census.gov geocoding API
# - GitHub congress-legislators repository
# - PMTiles for district boundaries
# - Census TIGERweb (state legislative district boundaries)

# =============================================================================
# Security / Authentication
# =============================================================================

# Secret token for cache warming endpoint (prevents unauthorized cache busting)
# Generate a secure random token: openssl rand -base64 32
CACHE_WARM_SECRET=your_secure_random_token_here

# Admin API key for administrative endpoints (agent, cache admin, FEC health)
# Required in production. Generate: openssl rand -base64 32
ADMIN_API_KEY=your_admin_api_key_here

# Bearer token for authenticating Vercel cron job requests
# (nostr-publisher, bill-summarizer, rss-aggregator)
# Recommended in production. Generate: openssl rand -base64 32
CRON_SECRET=your_cron_secret_here

# Comma-separated list of allowed CORS origins (defaults to the app's own origin).
# ALLOWED_ORIGINS and CORS_ALLOWED_ORIGINS are both honored.
# ALLOWED_ORIGINS=https://civdotiq.org,https://www.civdotiq.org
# CORS_ALLOWED_ORIGINS=https://civdotiq.org

# =============================================================================
# Application URLs
# =============================================================================

# Base URL for internal API calls and cache warming
# Set to your production domain in production
NEXT_PUBLIC_BASE_URL=http://localhost:3000

# Public-facing site URL for social sharing and OG images
# NEXT_PUBLIC_SITE_URL=https://civdotiq.org

# =============================================================================
# Operational Tuning (Optional - sensible defaults if unset)
# =============================================================================

# FEC shared-key rate limiter: per-minute request budget reserved for cron jobs
# (live user traffic is prioritized). Default tuned to the observed 60/min key cap.
# FEC_CRON_BUDGET_PER_MIN=30

# Intelligence cache warmer (cron): representatives processed concurrently and
# per-run slice size. Lower these if the warm-intelligence cron hits timeouts.
# WARM_INTEL_REP_CONCURRENCY=3
# WARM_INTEL_SLICE_SIZE=25

# =============================================================================
# Observability / Analytics (Optional - all off unless set)
# =============================================================================

# Error tracking and APM. Each is enabled only when its var is present.
# SENTRY_DSN=https://...@oXXXX.ingest.sentry.io/XXXX
# DATADOG_API_KEY=your_datadog_api_key_here
# NEW_RELIC_LICENSE_KEY=your_new_relic_license_key_here

# Web analytics.
# GA_TRACKING_ID=G-XXXXXXXXXX
# PLAUSIBLE_DOMAIN=civdotiq.org

# =============================================================================
# Development Notes
# =============================================================================

# 1. Most API keys are free to obtain
# 2. Rate limits are generous for civic engagement applications
# 3. When APIs are unavailable, endpoints return empty data — never fabricated data
# 4. No sensitive user data is stored or transmitted
# 5. All API communications use HTTPS
